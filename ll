	// index = a_pos_in_a(stack_a, num);
	// instructions[0] = stack_a->top - ia; ra
	// instructions[1] = ia + 1; rra
	// index = a_pos_in_b(stack_b, num);
	// instructions[2] = stack_b->top - ib + 1; rb
	// instructions[4] = ib; rrb
